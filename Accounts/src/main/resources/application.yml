server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: am
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active:
      - "qa"

build:
  version: "1.1.1"

accounts:
  message: "Welcome to Banking-App-2 accounts related local APIs "
  contactDetails:
    name: "Andy - Developer"
    email: "andy@test.com"
  onCallSupport:
    - (555) 555-1234
    - (555) 523-1345